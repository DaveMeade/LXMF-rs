# v0.9.9-rc.2 Release Candidate Evidence Ledger

Status: **IN PROGRESS — rc.1 superseded by rc.2; hosted and artifact evidence not yet recorded**

This ledger is the release record for the RNS 1.4.2 software-parity candidate.
It must be updated after the tag-triggered workflows complete. A skipped,
missing, unavailable, or optional check is recorded explicitly; it is not
silently converted to PASS.

## Candidate identity

| Field | Value |
|---|---|
| Candidate tag | `v0.9.9-rc.2` |
| Release commit | `NOT YET RECORDED — set to the exact tag target SHA` |
| Workspace/package version | `0.9.9` |
| Current branch base | `main` |
| Prior stable | `v0.9.8` |
| RNS reference | `1.4.2` at `b48b96e61676504e0a4e527b33b9a0b4495c6872` |
| LXMF reference | `727830cefda83d9c6e3982b48675425f3f988f9c` |
| Release notes | [`docs/release-notes-v0.9.9-rc.2.md`](../release-notes-v0.9.9-rc.2.md) |

## Parity target

| Total | Applicable | Complete | Partial | Unmapped | Not applicable |
|---:|---:|---:|---:|---:|---:|
| 1,811 | 1,810 | 1,810 | 0 | 0 | 1 |

The generated source of truth is
[`docs/status/python-surface-parity.json`](python-surface-parity.json). The
single not-applicable entry is the provenance-backed absent `CRNS` package.
The seven tracked LXMF module rows are complete for their named software
scenarios. Hardware, public-network, and third-party-client validation are
independent evidence axes.

## Documentation and metadata reconciliation

| Area | Result |
|---|---|
| `VERSION` and publishable workspace packages | PASS — aligned to `0.9.9`; private `test-support`/`xtask` package versions remain `0.1.0` by convention |
| RNS row-level matrix | PASS — software rows are complete; evidence boundaries are explicit |
| LXMF row-level matrix | PASS — all seven tracked rows remain complete |
| Current roadmap | PASS — `v0.9.9-rc.2` is current; v0.9.8 is historical |
| Release workflow docs/template | PASS — RC naming, MSI numeric version, stable-only channels, signing limits, and artifact names reconciled |
| Historical v0.9.8 records | PASS — retained without rewriting their release-boundary counts |

## Local validation

Record the exact command and result on the reviewed release commit.

| Gate | Result | Evidence |
|---|---|---|
| Python inventory self-test | PASS — preliminary | `python tools/scripts/python_surface_inventory.py --self-test` |
| Python inventory exact target | PASS — preliminary | `python tools/scripts/python_surface_inventory.py --check --json-out docs/status/python-surface-parity.json` |
| Explicit pinned-reference inventory | NOT YET RECORDED | Must use the pinned Reticulum/LXMF paths and `--require-complete` |
| `cargo fmt --all -- --check` | NOT YET RECORDED |  |
| `cargo check --workspace --all-targets` | NOT YET RECORDED |  |
| `cargo test --workspace --tests` | NOT YET RECORDED |  |
| `cargo clippy --workspace --all-targets --all-features --no-deps -- -D warnings` | NOT YET RECORDED |  |
| `cargo doc --workspace --no-deps` | NOT YET RECORDED |  |
| `tools/scripts/check-boundaries.sh` | NOT YET RECORDED |  |
| `cargo run -p xtask -- architecture-checks` | NOT YET RECORDED |  |
| `cargo xtask ci` | NOT YET RECORDED |  |
| `cargo xtask release-check` | NOT YET RECORDED |  |
| performance dashboard/check scripts | NOT YET RECORDED |  |

## Hosted workflows

| Workflow | Run ID / URL | Result |
|---|---|---|
| CI | NOT YET RECORDED | NOT YET TESTED on the RC tag commit |
| Verify / HIL PR level | NOT YET RECORDED | NOT YET TESTED on the RC tag commit |
| Release | NOT YET RECORDED | NOT YET TESTED |
| Performance release | NOT YET RECORDED | NOT YET TESTED |
| Security / scorecard / leader readiness | NOT YET RECORDED | NOT YET TESTED or workflow-specific |

The repository-owned HIL controller must be read by scenario, not reduced to one
green badge. Its current evidence boundaries include the reference-only boundary
case and the Python-to-Rust remote-relay case when those steps remain deselected
or skipped.

## Expected release assets

The release workflow is expected to publish these classes of assets; the final
ledger must list the actual asset names and mark every missing item.

| Class | Expected | Produced |
|---|---|---|
| Linux archives | x86_64, aarch64, armv7 musl `.tar.gz` | NOT YET VERIFIED |
| Windows archives | x86_64 `.zip` and numeric-version MSI | NOT YET VERIFIED |
| macOS archive | universal `.tar.gz` | NOT YET VERIFIED |
| Debian/RPM packages | amd64, arm64, armhf `.deb` and `.rpm` | NOT YET VERIFIED |
| SBOMs | three Rust CycloneDX files plus container CycloneDX | NOT YET VERIFIED |
| Checksums | `SHA256SUMS.txt` | NOT YET VERIFIED |
| Checksum signature | `SHA256SUMS.txt.cosign.bundle` | NOT YET VERIFIED |
| File provenance | GitHub attestations for release files | NOT YET VERIFIED |
| Performance dashboard | `lxmf-rs-performance.json` and `.html` plus checksum where emitted | NOT YET VERIFIED |

## Signing and provenance

| Surface | Result |
|---|---|
| Windows/Azure Trusted Signing | OPTIONAL — record signed or unsigned with the workflow reason |
| macOS codesigning | OPTIONAL — record signed or unsigned; notarization is not wired |
| Release checksum cosign bundle | NOT YET VERIFIED |
| GitHub release-file attestations | NOT YET VERIFIED |
| OCI cosign signature | NOT YET VERIFIED |
| OCI build-provenance attestation | NOT YET VERIFIED |

## OCI publication

| Field | Result |
|---|---|
| Version tag | `ghcr.io/freetakteam/lxmf-rs:0.9.9-rc.2` — NOT YET VERIFIED |
| Stable `latest` tag | MUST NOT move for this RC; NOT YET VERIFIED |
| Platforms | `linux/amd64`, `linux/arm64` — NOT YET VERIFIED |
| SBOM | NOT YET VERIFIED |
| Signature and attestation | NOT YET VERIFIED |

## Performance dashboard

| Field | Result |
|---|---|
| Workflow run | NOT YET RECORDED |
| JSON asset | NOT YET VERIFIED; unavailable cells must be `N/A` |
| HTML asset | NOT YET VERIFIED |
| Dashboard checksum | NOT YET VERIFIED |
| Numeric interpretation | No new numbers are claimed until the tag workflow emits the dataset |

## Deferred evidence boundaries

These are explicitly deferred and do not downgrade the software inventory:

- physical RNode, RNodeMulti, Weave, VR-N76, BLE, and serial-radio validation;
- public I2P and public Reticulum network operation/soak;
- Sideband, MeshChatX, Columba, and other third-party-client compatibility;
- the pinned workflow's reference-only boundary case and Python-to-Rust
  remote-relay scenario when they are not executed.

## Recommendation

**NOT YET READY** — the software parity target and documentation reconciliation
are prepared, but the candidate cannot be recommended until the local gates,
tagged hosted workflows, artifact set, signatures/attestations, OCI state, and
performance result are verified on the exact immutable tag commit.
